<Page
    x:Class="UWPCommunity.Views.UnhandledExceptionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPCommunity.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemAccentColor}">

    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Page.Resources>

    <Grid Padding="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="100" Grid.Row="0" Text=":("/>
        <TextBlock FontSize="24" FontWeight="SemiBold" Grid.Row="1"
                   Text="UWP Community ran into an unexpected error." TextWrapping="Wrap"/>
        <TextBox Grid.Row="2" Text="{x:Bind Path=ExceptionText}" AcceptsReturn="True" IsReadOnly="True" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap">
            <TextBox.Header>
                <TextBlock Text="Exception:"/>
            </TextBox.Header>
        </TextBox>
    </Grid>
</Page>
