<Page
    x:Class="UWPCommunity.Views.Subviews.LlamaBingo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPCommunity.Views.Subviews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:UWPCommunity.Controls"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Grid>
        <Button>
            <Button.Flyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Copy Link" Click="CopyLink_Click">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Link"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Save As Image" Click="SaveImage_Click">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Save"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </Button.Flyout>
            <Button.Content>
                <StackPanel Spacing="5" Orientation="Horizontal">
                    <SymbolIcon Symbol="Share"/>
                    <TextBlock Text="Share board"/>
                </StackPanel>
            </Button.Content>
        </Button>
        <Viewbox>
            <controls:BingoCard x:Name="Bingo" Width="600" Height="600"/>
        </Viewbox>
    </Grid>
    
</Page>
