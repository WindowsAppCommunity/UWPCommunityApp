<Page
    x:Class="UWPCommunity.Views.ProjectsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPCommunity.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:UWPCommLib.Api.UWPComm.Models"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls1="using:UWPCommunity.Controls"
    mc:Ignorable="d">

    <Grid>

        <controls:AdaptiveGridView ItemsSource="{x:Bind Projects}" SelectionMode="Single" DesiredWidth="600">
            <controls:AdaptiveGridView.ItemTemplate>
                <DataTemplate x:DataType="models:Project">

                    <controls1:GridViewCardItem TitleText="{x:Bind AppName}"
                                                BodyText="{x:Bind Description}"/>

                </DataTemplate>
            </controls:AdaptiveGridView.ItemTemplate>
        </controls:AdaptiveGridView>
        
    </Grid>
</Page>
