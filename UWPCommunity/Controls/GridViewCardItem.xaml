<UserControl
    x:Class="UWPCommunity.Controls.GridViewCardItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPCommunity.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <controls:DockPanel LastChildFill="True" Padding="5,5,5,5" HorizontalAlignment="Stretch">

        <controls:DockPanel.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem Text="Edit" Icon="Edit" Command="{x:Bind EditCommand}"/>
                <MenuFlyoutItem Text="Delete" Icon="Delete" Command="{x:Bind DeleteCommand}"/>
                <MenuFlyoutItem Text="View" Icon="View" Command="{x:Bind ViewCommand}"/>
            </MenuFlyout>
        </controls:DockPanel.ContextFlyout>

        <StackPanel controls:DockPanel.Dock="Bottom" Spacing="5"
                    VerticalAlignment="Stretch" Orientation="Horizontal">
            <Button x:Name="ViewButton" Command="{x:Bind ViewCommand}">
                <SymbolIcon Symbol="View"/>
            </Button>
            <Button x:Name="EditButton" Command="{x:Bind EditCommand}">
                <SymbolIcon Symbol="Edit"/>
            </Button>

            <Button x:Name="DeleteButton" Command="{x:Bind DeleteCommand}">
                <SymbolIcon Symbol="Delete"/>
            </Button>
        </StackPanel>

        <Image MaxWidth="150" Margin="0,0,5,0" controls:DockPanel.Dock="Left"
               Source="{x:Bind ImageSource}"/>

        <StackPanel Orientation="Vertical">
            <TextBlock Text="{x:Bind TitleText}" Margin="0,0,5,0"
                       Style="{StaticResource BaseTextBlockStyle}" />
            <TextBlock Text="{x:Bind BodyText}" Margin="0,0,5,0"
                       Style="{StaticResource BodyTextBlockStyle}" TextWrapping="WrapWholeWords"/>
        </StackPanel>

    </controls:DockPanel>
    
</UserControl>
